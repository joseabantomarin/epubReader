# Data model

The source of truth is `server/src/db.js` (SQLite, `foreign_keys = ON`). This
diagram is generated by hand from that schema; update it when the schema changes.

```mermaid
erDiagram
    users ||--o{ books          : "owns"
    users ||--o{ annotations    : "writes"
    users ||--o{ ratings        : "gives"
    users ||--o{ groups         : "owns"
    users |o--o{ group_members  : "is (user_id nullable)"
    books ||--o| reading_progress : "has"
    books ||--o{ annotations    : "marked up by"
    books ||--o{ ratings        : "rated by"
    groups ||--o{ group_members : "contains"
    groups |o..o{ books         : "shared to (soft, visibility=group)"
    users  |o..o{ books         : "shared to (soft, visibility=user)"

    users {
        INTEGER id PK
        TEXT    google_sub UK "Google account subject id"
        TEXT    email
        TEXT    name
        TEXT    picture_url
        TEXT    created_at
    }

    books {
        INTEGER id PK
        INTEGER user_id FK "owner -> users.id, ON DELETE CASCADE"
        TEXT    title
        TEXT    author
        TEXT    cover_path
        TEXT    file_path "relative path under data/books/<userId>/"
        INTEGER file_size
        TEXT    format "epub | pdf"
        INTEGER shared "legacy flag, 1 = public"
        INTEGER censored "admin hidden"
        TEXT    censor_reason
        TEXT    visibility "private | public | group | user"
        INTEGER share_group_id "soft ref -> groups.id (when visibility=group)"
        INTEGER share_user_id "soft ref -> users.id (when visibility=user)"
        TEXT    uploaded_at
    }

    reading_progress {
        INTEGER book_id PK "-> books.id, ON DELETE CASCADE"
        TEXT    cfi "EPUB canonical fragment identifier"
        REAL    percentage
        INTEGER total_pages
        TEXT    last_read_at
    }

    annotations {
        INTEGER id PK
        INTEGER book_id FK "-> books.id, ON DELETE CASCADE"
        INTEGER user_id FK "-> users.id, ON DELETE CASCADE"
        TEXT    cfi
        TEXT    text "highlighted text"
        TEXT    note "user note"
        TEXT    color
        TEXT    chapter
        INTEGER page
        TEXT    created_at
        TEXT    updated_at
    }

    ratings {
        INTEGER book_id FK "-> books.id, ON DELETE CASCADE"
        INTEGER user_id FK "-> users.id, ON DELETE CASCADE"
        INTEGER stars "CHECK 1..5"
        TEXT    updated_at
    }

    groups {
        INTEGER id PK
        INTEGER owner_id FK "-> users.id, ON DELETE CASCADE"
        TEXT    name
        TEXT    created_at
    }

    group_members {
        INTEGER id PK
        INTEGER group_id FK "-> groups.id, ON DELETE CASCADE"
        INTEGER user_id FK "-> users.id, ON DELETE CASCADE, NULLABLE"
        TEXT    email "invite target; UNIQUE(group_id, email)"
        TEXT    created_at
    }
```

## Notes

- **Cardinality legend:** `||` one, `o|` zero-or-one, `o{` zero-or-many. Solid
  lines are DB-enforced foreign keys; dashed lines (`..`) are *soft* references
  stored as plain integer columns with no `REFERENCES` constraint.
- **Composite keys.** `reading_progress` is keyed by `book_id` (one row per
  book). `ratings` has a composite primary key `(book_id, user_id)`, so a user
  rates a given book at most once.
- **Cascade deletes.** Deleting a `users` row cascades to that user's `books`,
  `annotations`, `ratings`, owned `groups`, and their `group_members` rows;
  deleting a `books` row cascades to its `reading_progress`, `annotations`, and
  `ratings`. This is what powers account deletion (`DELETE /api/auth/account`).
- **Soft references are not cascaded.** Because `books.share_group_id` and
  `books.share_user_id` are not real foreign keys, deleting a group or a share
  target leaves them dangling; the account-deletion path resets those books back
  to `visibility='private'` explicitly rather than relying on the database.
- **Pending invitations.** `group_members.user_id` is nullable: a row can exist
  for an invited `email` before that person has an account, and is linked to a
  real `user_id` on their first login (`linkPendingMemberships`).
- **Visibility model.** `books.visibility` (`private | public | group | user`)
  is the current sharing control; `books.shared` is the legacy boolean kept in
  sync for public books. `share_group_id` / `share_user_id` carry the target
  when visibility is `group` / `user`.
