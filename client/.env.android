# Native (Capacitor) release build against the production backend.
# Loaded by: vite build --mode android  (npm run build:android)
# Safe to commit: VITE_* values are baked into the shipped bundle and are public
# by design. NEVER put real secrets here (those live in server/.env).
VITE_GOOGLE_CLIENT_ID=823603281404-rgg8sb970f86cmqo91vgi6ibh0ph8ban.apps.googleusercontent.com
VITE_API_BASE=https://mislibros.openlinks.app
